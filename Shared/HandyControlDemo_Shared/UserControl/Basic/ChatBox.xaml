<UserControl
    x:Class="HandyControlDemo.UserControl.ChatBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ex="clr-namespace:HandyControlDemo.Tools.Extension"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:langs="clr-namespace:HandyControlDemo.Properties.Langs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="300"
    Height="486"
    Background="{DynamicResource RegionBrush}"
    DataContext="{Binding ChatBox, Source={StaticResource Locator}}"
    Effect="{StaticResource EffectShadow5}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ListBox
            Name="ListBoxChat"
            Background="Transparent"
            BorderThickness="0"
            ItemsSource="{Binding ChatInfos}"
            ScrollViewer.CanContentScroll="False"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            Style="{StaticResource StackPanelVerticalListBox}">
            <hc:Interaction.Triggers>
                <hc:RoutedEventTrigger RoutedEvent="hc:ChatBubble.Selected">
                    <hc:EventToCommand Command="{Binding ReadMessageCmd}" PassEventArgsToCommand="True" />
                </hc:RoutedEventTrigger>
            </hc:Interaction.Triggers>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <hc:ChatBubble
                        MaxWidth="300"
                        Content="{Binding Message}"
                        Role="{Binding Role}"
                        Tag="{Binding}"
                        Type="{Binding Type}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Border Grid.Row="1" Background="{DynamicResource DarkDefaultBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ToggleButton
                    Name="ToggleButtonAudio"
                    Width="30"
                    Height="30"
                    Margin="6"
                    Padding="7"
                    hc:BorderElement.CornerRadius="15"
                    hc:IconSwitchElement.Geometry="{StaticResource AudioGeometry}"
                    hc:IconSwitchElement.GeometrySelected="{StaticResource KeyboardGeometry}"
                    Style="{StaticResource ToggleButtonIcon}" />
                <Button
                    Grid.Column="2"
                    Width="30"
                    Height="30"
                    Margin="6,0,6,0"
                    Padding="7"
                    hc:BorderElement.CornerRadius="15"
                    hc:IconElement.Foreground="White"
                    hc:IconElement.Geometry="{StaticResource ImageGeometry}"
                    Command="{Binding OpenImageCmd}" />
                <TextBox
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{Binding ChatString, UpdateSourceTrigger=PropertyChanged}"
                    Visibility="{Binding IsChecked, ElementName=ToggleButtonAudio, Converter={StaticResource Boolean2VisibilityReConverter}}">
                    <hc:Interaction.Triggers>
                        <hc:EventTrigger EventName="KeyDown">
                            <hc:EventToCommand Command="{Binding SendStringCmd}" PassEventArgsToCommand="True" />
                        </hc:EventTrigger>
                    </hc:Interaction.Triggers>
                </TextBox>
                <Button
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    Content="{ex:Lang Key={x:Static langs:LangKeys.PushToTalk}}"
                    Visibility="{Binding IsChecked, ElementName=ToggleButtonAudio, Converter={StaticResource Boolean2VisibilityConverter}}">
                    <hc:Interaction.Triggers>
                        <hc:EventTrigger EventName="PreviewMouseLeftButtonDown">
                            <hc:EventToCommand Command="{Binding StartRecordCmd}" />
                        </hc:EventTrigger>
                        <hc:EventTrigger EventName="PreviewMouseLeftButtonUp">
                            <hc:EventToCommand Command="{Binding StopRecordCmd}" />
                        </hc:EventTrigger>
                    </hc:Interaction.Triggers>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
